---
id: f9f42c9f
raw_id: f9f42c9f-6f24-4e87-85eb-4f0391c0b434
para: Archives
tags: ["archives"]
summary: "# SecondSelf — Phase 0 / Phase 1 Quick start 1. Create a virtual environment and install requirements: ```powershell python -m venv .venv .venv\Scripts\Activate.ps1 pip install ..."
created: 2026-07-31T17:21:59.841694+00:00
links: []
---

# SecondSelf — Phase 0 / Phase 1

Quick start

1. Create a virtual environment and install requirements:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

2. Capture text from the CLI:

```powershell
python -m src.capture --text "Buy milk" --source todo
```

3. Capture a file:

```powershell
python -m src.capture --file C:\path\to\file.jpg --source file_import
```

Captured items are stored in the `raw/` directory and metadata is in `raw/metadata.json`.

